# ReVIEW.js をNode.js上で動かすサンプル

以下のコマンドを実行する。
$ npm install

以下のコマンドを実行してコンパイルする。
$ ./node_modules/.bin/reviewjs

もし、npm install -g review.js した場合は以下でもよい。
$ reviewjs

もし、npmが入っていない場合はNode.jsをインストールする。
Mac, Linux環境にはnodebrewがおすすめ。
